apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: website
  namespace: ecommerce-test
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-certificate
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - okane.no
        - www.okane.no
      secretName: okane-tls
    - hosts:
        - okane-website.marensiuspettersen.no
      secretName: okane-website-marensiuspettersen-tls
  rules:
    - host: okane.no
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  number: 80
    - host: www.okane.no
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  number: 80
    - host: okane-website.marensiuspettersen.no
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  number: 80
