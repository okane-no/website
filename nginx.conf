server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css text/javascript application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # Immutable cache for hashed Astro assets
    location /_astro/ {
        expires max;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Remove trailing slashes
    rewrite ^/(.*)/$ /$1 permanent;

    # Clean URLs: try exact file, then .html, then directory index.
    # no-cache so browsers revalidate HTML on every visit; hashed assets
    # under /_astro/ keep their immutable cache from the block above.
    location / {
        add_header Cache-Control "no-cache";
        try_files $uri $uri.html $uri/index.html =404;
    }

    error_page 404 /404.html;
}
